# WOW MAKERS | Premium Event Management

This project contains the WOW MAKERS website.

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
